# Todos 005, 006, 017 — Result

## Files Changed

- `src/lib/actions/admin.ts`
- `src/app/api/cron/resolve-markets/route.ts`
- `src/db/schema.ts`

## Summary of Changes

### Todo 005 — Block cancellation of resolving/failed markets
**File:** `src/lib/actions/admin.ts` (~line 177)

Changed the cancel guard from checking only `"resolved"` and `"cancelled"` to also block `"resolving"` and `"failed"` statuses using an array `.includes()` check. Updated the error message to reflect all blocked statuses.

### Todo 006 — Add catch-all active→resolving transition in cron
**File:** `src/app/api/cron/resolve-markets/route.ts` (inserted before step 3)

Added a third bulk transition after the existing halted→resolving step. This safety-net query transitions any market still in `"active"` status whose `resolvesAt` has passed, catching markets that missed the halt window entirely.

### Todo 017 — Fix outcome semantics comments
**File:** `src/db/schema.ts`

Fixed three comments that incorrectly stated `0=NO, 1=YES`:
- Line ~101: `markets.outcome` column (null until resolved)
- Line ~135: `positions.outcome` column
- Line ~163: A second `outcome` column in a related table (also corrected via replace_all)

All now correctly read `// 0=YES, 1=NO`, consistent with `quantityYes` being at index 0.

## Blockers

None.
